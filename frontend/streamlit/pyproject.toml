[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "frontend-streamlit"
version = "0.1.0"
description = "Streamlit frontend UI for Finance Agents."
requires-python = ">=3.12,<3.13"
dependencies = [
    "streamlit",
    "requests"
]
